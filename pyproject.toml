[project]
name = "work"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "minskyong", email = "fiterpen9@naver.com" }
]
requires-python = ">=3.14"
dependencies = [
    "seaborn>=0.13.2",
]

[project.scripts]
work = "work:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
